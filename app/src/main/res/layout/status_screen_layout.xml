<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View" />
        <import type="com.example.viwiki.network.ApiUtils.ApiStatus" />
        <variable
            name="viewModel"
            type="com.example.viwiki.home.HomeViewModel" />
    </data>
    <LinearLayout
        android:id="@+id/status_screen"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="visible"
        tools:visibility="visible"
        app:status="@{viewModel.status}"
        tools:showIn="@layout/fragment_home">

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{viewModel.status == ApiStatus.LOADING ? View.VISIBLE : View.INVISIBLE}" />

        <TextView
            android:id="@+id/tv_info_message_string"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:status="@{viewModel.status}"
            android:textAlignment="center"
            android:textSize="@dimen/header2_size"
            android:visibility="visible"/>

        <Button
            android:id="@+id/btn_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="try again"
            android:visibility="@{viewModel.status == ApiStatus.ERROR ? View.VISIBLE : View.INVISIBLE}" />

    </LinearLayout>
</layout>